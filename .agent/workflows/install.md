---
description: Install project dependencies
---

# Install Dependencies

Install all npm dependencies for the project.

## Steps

// turbo
1. Install dependencies:
```bash
npm install
```

## Notes
- Run this after cloning the repo or when `package.json` changes
- If you encounter issues, try deleting `node_modules` and `package-lock.json` first
